#include <stdio.h>

/*SEE ALGORITHM DESIGN
Also, I think I need to use arrays of pointers to store the lines from the gamefile, because I will need to store large amounts of text. Then I can also compare strings easily. Use structs? Not sure. Look them up. 
*/

int main(){
    //ints

    //chars

    //FILEs
    FILE *gameFile;
    FILE *notebook;
    FILE *lockTried;
    FILE *


}
